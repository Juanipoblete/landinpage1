<h1>Detalles de Staff</h1>

<form>
  <select>
    <% @roles.each do |t| %> 
    <option value="<%= t.id %>"><%= t.nombre %></option>
    <% end %>
  </select>
  <button class="btn btn-primary">Guardar Rol</button>
</form>
